
export const InitState = {
  builtins: [],
  plugins: [],
  directory: '',
};

export default function reducer(state = InitState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
